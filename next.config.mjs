/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org', 'i.scdn.co', 'cdns-images.dzcdn.net', "s3.amazonaws.com", "static.wikia.nocookie.net", "media.them.us", "avatar.iran.liara.run"],
    },
  };
  
  export default nextConfig;